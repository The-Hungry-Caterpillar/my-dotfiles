env:
  # This value is used to set the `$TERM` environment variable for
  # each instance of Alacritty. If it is not present, alacritty will
  # check the local terminfo database and use `alacritty` if it is
  # available, otherwise `xterm-256color` is used.
  #TERM: xterm-256color
  TERM: alacritty

cursor:
  # Block Underline or Beam
  style: Block

font:
  normal:
    # Needs to be powerline font if using zsh theme with powerline
    family: Ubuntu Mono derivative Powerline
  # Font size
  size: 18
  # Better font rendering for mac
  use_thin_strokes: true

election:
  # This string contains all characters that are used as separators for
  # "semantic words" in Alacritty.
  semantic_escape_chars: ",│`|:\"' ()[]{}<>\t"

  # When true, selected text will be copied to the primary clipboard
  save_to_clipboard: true

window:
  # Spread additional padding evenly around the terminal content.
  padding:
    x: 10
    y: 10
  # Remove buttons
  decorations: none
  # Opacity
  #opacity: 0.9
  opacity: 1
  # Dimensions if not SimpleFullscreen
  dimensions:
    columns: 86
    lines: 50

import:
  # Entire list:
    # Cobalt2.yaml
    # afterglow.yaml
    # argonaut.yaml
    # atom_one_light.yaml
    # ayu_dark.yaml
    # base16_default_dark.yaml
    # blood_moon.yaml
    # breeze.yaml
    # campbell.yaml
    # challenger_deep.yaml
    # cyber_punk_neon.yaml
    # darcula.yaml
    # dark_pastels.yaml
    # doom_one.yml
    # dracula.yaml
    # falcon.yaml
    # flat_remix.yml
    # gotham.yaml
    # gruvbox_dark.yaml
    # gruvbox_light.yaml
    # gruvbox_material.yml
    # high_contrast.yaml
    # horizon-dark.yaml
    # hyper.yaml
    # iterm.yaml
    # konsole_linux.yaml
    # low_contrast.yaml
    # material_theme.yaml
    # material_theme_mod.yaml
    # monokai_pro.yaml
    # moonlight_ii_vscode.yaml
    # night_owlish_light.yaml
    # nord.yaml
    # oceanic_next.yaml
    # omni.yml
    # one_dark.yaml
    # palenight.yml
    # papercolor_dark.yaml
    # papercolor_light.yaml
    # pencil_dark.yaml
    # pencil_light.yaml
    # remedy_dark.yaml
    # seashells.yaml
    # smoooooth.yml
    # snazzy.yaml
    # solarized_dark.yaml
    # solarized_light.yaml
    # taerminal.yaml
    # tango_dark.yaml
    # tender.yaml
    # terminal_app.yaml
    # thelovelace.yaml
    # tokyo-night-storm.yaml
    # tokyo-night.yaml
    # tomorrow_night.yaml
    # tomorrow_night_bright.yaml
    # wombat.yaml
    # xterm.yaml
  # Favorites:
  #   remedy_dark.yaml -- rich looking
  #   palenight.yml -- goes well with PaperColor
  #   horizon_dark.yaml
  #   ayu_dark.yaml # stock looking but dark and nicer, probs my fave
  #   one_dark.yaml
  - /Users/jogata/src/alacritty-theme/themes/ayu_dark.yaml
